FILE(GLOB TARGET_SRC *.cpp)
FILE(GLOB TARGET_H *.h)

OSG_COMPILE_CONCAT_GLOB(TARGET_SRC "${CMAKE_CURRENT_SOURCE_DIR}/LibraryWrapper.cpp")
SET(TARGET_ADDED_LIBRARIES osgText )

#### end var setup  ###
SETUP_PLUGIN(osgtext)
