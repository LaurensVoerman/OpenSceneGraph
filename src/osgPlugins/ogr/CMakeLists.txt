INCLUDE_DIRECTORIES( ${GDAL_INCLUDE_DIR} )

SET(TARGET_SRC ReaderWriterOGR.cpp)
IF(WIN32 OR MINGW)
    SET(TARGET_EXTERNAL_LIBRARIES ${TARGET_EXTERNAL_LIBRARIES} odbc32 odbccp32)
ENDIF()
SET(TARGET_LIBRARIES_VARS GDAL_LIBRARY )

#### end var setup  ###
SETUP_PLUGIN(ogr)
